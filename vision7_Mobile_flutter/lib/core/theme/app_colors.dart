import 'package:flutter/material.dart';

class AppColors {
  // Surfaces
  static const dark = Color(0xFF1A1A1A);
  static const darkLight = Color(0xFF252525);
  static const darkHover = Color(0xFF2A2A2A);
  static const cream = Color(0xFFF5F0EB);
  static const warm = Color(0xFFE8E0D8);
  static const warmLight = Color(0xFFF0EAE3);

  // Light theme surfaces
  static const surface = Color(0xFFF5F0EB);
  static const surfaceCard = Color(0xFFFFFFFF);
  static const surfaceElevated = Color(0xFFF0EAE3);
  static const surfaceDivider = Color(0xFFE8E0D8);

  // Text
  static const text = Color(0xFF2D2D2D);
  static const textOnDark = Color(0xFFF5F0EB);
  static const muted = Color(0xFF6B6B6B);
  static const mutedOnDark = Color(0xFF9A9A9A);
  static const white = Color(0xFFFFFFFF);
  static const black = Color(0xFF000000);

  // Accent
  static const gold = Color(0xFFFFCF01);
  static const goldDark = Color(0xFFBFA302);
  static const goldLight = Color(0xFFFFF3B0);

  // Status
  static const success = Color(0xFF4ADE80);
  static const successDark = Color(0xFF22C55E);
  static const error = Color(0xFFEF4444);
  static const errorDark = Color(0xFFDC2626);
  static const errorLight = Color(0xFFEF5350);
  static const warning = Color(0xFFF59E0B);
  static const info = Color(0xFF3B82F6);

  // Notification badge colors
  static const notificationPurple = Color(0xFF9333EA);
  static const notificationCyan = Color(0xFF06B6D4);
  static const notificationOrange = Color(0xFFEA580C);

  // Grayscale
  static const grayDark = Color(0xFF333333);
  static const grayMedium = Color(0xFFB0B0B0);
  static const grayBorder = Color(0xFF3A3A3A);

  // Overlay
  static const overlay = Color(0x99000000);
  static const overlayLight = Color(0x4D000000);

  // Academy theme
  static const academyNavy = Color(0xFF011B2B);
  static const academySurface = Color(0xFF011B2B);
  static const academyInputFill = Color(0xFF0A2438);
  static const academyGold = Color(0xFFFFCF01);
  static const academyGoldDark = Color(0xFFC9A900);
  static const academyWhite = Color(0xFFFFFFFF);
  static const navy = Color(0xFF011B2B);
}
